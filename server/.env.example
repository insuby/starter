# Порт и хост API
PORT=3001
HOST=0.0.0.0

# CORS: адрес фронтенда (Vite dev — http://localhost:5173). * — для разработки.
CORS_ORIGIN=*

# PostgreSQL. Если не задан — сервер работает на in-memory данных (демо/тесты).
# DATABASE_URL=postgres://bso:bso@localhost:5432/bso

# Уровень логирования fastify
LOG_LEVEL=info
